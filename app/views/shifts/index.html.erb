<div class="table-view">
  <% @shifts.each do |shift| %>
    <%= link_to shift_path(shift), class: "table-view-cell" do %>
      <div class="cell-body">
        <p><%= shift.title %></p>
        <small>Tasks: <%= shift.assignments.count %></small>
      </div>
    <% end %>
  <% end %>
</div>
